import { HomeCheffProjectHub } from "@/components/projects/homecheff-project-hub";

/** Legacy HomeCheff local package hub — preserved under S2H. */
export default function ProjectPackagesPage() {
  return <HomeCheffProjectHub />;
}
