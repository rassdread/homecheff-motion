"use client";

import { StudioHomeDashboard } from "@/components/studio/studio-home-dashboard";

/** Legacy alias — redirects to /studio in page.tsx; kept for direct imports. */
export function StudioMyStudioPage() {
  return <StudioHomeDashboard />;
}
