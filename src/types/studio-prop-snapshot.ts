/**
 * Portable prop identity for future Motion / Scene Composer integration.
 */

export type PropSnapshot = {
  id: string;
  name: string;
  category: string;
  description: string;
  referenceImageUrl: string;
};
