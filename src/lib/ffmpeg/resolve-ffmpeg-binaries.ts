/**
 * @deprecated Import from `@/lib/ffmpeg/resolve-app-ffmpeg` for new code.
 * Re-export shim so existing imports stay on the app resolver (no ffmpeg-static).
 */
export * from "@/lib/ffmpeg/resolve-app-ffmpeg";
