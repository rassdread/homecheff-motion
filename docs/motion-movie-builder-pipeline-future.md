# Movie Builder → Motion → Render pipeline (future)

Documented target flow (not fully wired in V18):

1. **Movie Builder** — prepare, generate, analyze, improve, select scenes
2. **Motion QA** — import handoff; V18 intelligence panel + pre-render readiness warning
3. **Render** — Instant Premium checkout / test generate (unchanged Vidu path)
4. **Final video** — assembly and delivery

V18 implements the **Motion QA** visibility step only. Automated gating between steps is future work.
