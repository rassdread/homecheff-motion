# Suite Productization Foundation

**Status:** Complete

Module ids (`editor`, `studio`, `motion`, `presentation`, `assets`, `suite`), route access resolution, feature flags, usage categories, plan label keys. Billing enforcement flag off.

## Key file

`src/lib/suite-productization.ts`
