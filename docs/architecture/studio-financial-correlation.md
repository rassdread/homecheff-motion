# Studio Financial Correlation (S.8B)

Privacy-safe ids via `buildFinancialCorrelationId` — no email/PII.

Correlate across: generation intent → GenerationJob → reservation/capture → ProviderCostEvent → Stripe session / Auto Top-Up attempt → output asset.

S.8C consumes these links for margin analysis (no math in S.8B).
